<%= form_for(@shikaku) do |f| %>
  <% if @shikaku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shikaku.errors.count, "error") %> prohibited this shikaku from being saved:</h2>

      <ul>
      <% @shikaku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :detail %><br>
    <%= f.text_area :detail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
